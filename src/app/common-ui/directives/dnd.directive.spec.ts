import { DndDirective } from './DndDirective';

describe('DndDirective', () => {
  it('should create an instance', () => {
    const directive = new DndDirective();
    expect(directive).toBeTruthy();
  });
});
